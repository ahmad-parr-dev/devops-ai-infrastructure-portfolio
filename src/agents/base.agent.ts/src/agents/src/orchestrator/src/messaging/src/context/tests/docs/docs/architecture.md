="docs01"}
# Architecture

This system is built around three core layers:

1. Agents — encapsulated logic units
2. Orchestrator — manages execution flow
3. Messaging — enables communication between agents

The goal is to provide a scalable foundation for AI-native systems
